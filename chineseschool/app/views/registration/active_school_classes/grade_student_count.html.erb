<% @title = '年級人數清單' %>

<h2>千橡中文學校  <%=h @school_year.name %>學年度  年級人數清單</h2>

<table class="details-table" border="1px">
  <thead>
  <tr>
    <th>年級</th>
    <th>人數</th>
    <th>Maximum Size</th>
  </tr>
  </thead>
  <tbody>
  <% total_student_count = 0 %>
  <% @grade_student_counts.keys.sort.each do |grade_id| -%>
      <% grade = Grade.find(grade_id) %>
      <tr>
        <td><%=h grade.name %></td>
        <td><%= @grade_student_counts[grade_id] %></td>
        <% total_student_count += @grade_student_counts[grade_id] %>
        <td><%= grade.allowed_max_student_count(@school_year) %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<% if @ec_class_student_counts.any? %>
<br/>
<h3>EC 每日中文班</h3>
<table class="details-table" border="1px">
  <thead>
  <tr>
    <th>班級</th>
    <th>人數</th>
    <th>Maximum Size</th>
  </tr>
  </thead>
  <tbody>
  <% @ec_class_student_counts.keys.sort.each do |school_class_id| -%>
      <% ec_class = SchoolClass.find(school_class_id) %>
      <tr>
        <td><%=h ec_class.name %></td>
        <td><%= @ec_class_student_counts[school_class_id] %></td>
        <% total_student_count += @ec_class_student_counts[school_class_id] %>
        <td><%= ec_class.max_size %></td>
      </tr>
  <% end %>
  </tbody>
</table>
<% end %>

<br/>
Total Student Count: <%= total_student_count %>
<br/>
Current Time: <%= PacificTimeDisplay.display_now_in_pacific %>
